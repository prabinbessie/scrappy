name: Scrape NEPSE Market CSV

on:
  workflow_dispatch:
  schedule:
    - cron: "15,45 5-8 * * 0-4"
    - cron: "15 9 * * 0-4"

permissions:
  contents: write

jobs:
  scrape-market:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .

      - name: Run market scraper
        run: python -m scraper.cli market

      - name: Commit and push data
        run: |
          git config user.name "Prabin Bhandari"
          git config user.email "prabinbessie@users.noreply.github.com"
          git pull --rebase --autostash
          git add data/nepse/*.csv
          git commit -m "Update market data ($(TZ=Asia/Kathmandu date +'%Y-%m-%d %H:%M NPT'))" || echo "Nothing new to commit"
          git push
